请结合文案的ASR识别结果，设计一套重点强调词，这些重点强调词用于在视频画面上重点显示以引导观众注意力。

要求：
1. 强调词/短句控制在 2-8 个字为宜，最多不超过8个字
2. 短句示例：如"元和十年"、"永州城外"等来自原文的精炼短句
3. 每个内容需要标注重要等级和类型：
   - level: 重要等级
     - 0 - 10: 0为最高优先级，10为最低优先级
     - 诗名，诗句：0级，必须包含在识别结果中，且不限制长度
     - 其他关键词：1-10级，根据重要性结合全文重点词的适宜密度，给一个等级
   - type: 内容类型
     - 0: 诗名（如"江雪"）
     - 1: 诗句（如"千山鸟飞绝"、"孤舟蓑笠翁",连续的上下句不能拆开）
     - 2: 其它关键词
   - begin_time: 关键词开始时间(精确到关键词开始的begin_time)
   - end_time: 关键词结束时间(精确到关键词结束的end_time)
4. 返回格式为 JSON 数组，每个元素包含 keyword、level、type、begin_time、end_time 字段
5. keyword 必须是文案中存在的原句或词语，不要编造
6. 如果是诗句，请用换行符分割每句（如"千山鸟飞绝\\n万径人踪灭"）
7. 只返回 JSON 数组，不要其他内容

请提取需要强调的内容（返回JSON数组）：